# prod へのプラン実行用
name: "Terraform Plan (prod)"

on:
  pull_request:
    branches: [main]
    paths: ["infra/**"]

jobs:
  call-plan:
    uses: ./.github/workflows/_terraform-plan.yml
    with: { workspace: prod }
